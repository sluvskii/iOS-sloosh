import Foundation

enum AppSecrets {
    // In git repository: empty placeholder.
    // Injected securely strictly during GitHub Actions CI build on the official repository.
    static let apiKey: String = ""
}
